/target
/.env
config/tokens.json
# 除外したい .env系ファイル
.env*
!/.env.example

# MCP configuration (contains user-specific settings)
.mcp.json

# Caddy configuration (contains absolute paths)
caddyfile
!caddyfile.example

# Local certificates
*.pem

# Shuttle secrets files
Secrets.toml
Secrets.dev.toml
.shuttle*
Secrets*.toml
